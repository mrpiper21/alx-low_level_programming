#include <stdio.h>
/**
 *main - Entry point to the code
 *Description: 'displays the numvers of base 10'
 *Return: returns 0
 */
int main(void)
{
	int b;

	for (b = 0; b <= 9; b++)
	{
		printf("%d", b);
	}
	printf("\n");
	return (0);
}
